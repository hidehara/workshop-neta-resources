Make zip package with Pillow(Python Imaging Lib) for deploying to lambda

using lambci/docker-lambda docker container.


RUN:

$ docker build -t mylambda:build-python3.8-zip .

$ docker run --rm -v "$PWD":/var/task mylambda:build-python3.8-zip

